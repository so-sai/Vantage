[package]
name = "vantage-vfp"
version = "1.0.0"
edition = "2021"
description = "Vantage Federation Protocol — monotonic divergence field over independent committed histories"

[dependencies]
vantage-core = { path = "../vantage-core" }
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
thiserror = "1.0"
