[package]
name = "vantage-metrics"
version = "0.1.0"
edition = "2021"
description = "Core metric types and computation for Vantage — IAR, RRF, Reality Half-Life, Reality Yield"

[dependencies]
vantage-core = { path = "../vantage-core" }
serde = { version = "1.0", features = ["derive"] }
